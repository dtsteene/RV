\documentclass[border=8pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath, amssymb, bm}
\input{_uio_style}

\begin{document}
\begin{tikzpicture}[x=1cm, y=1cm]

  % Main coupled pair — these are the focus of the figure.
  \node[card-blue, minimum width=54mm, minimum height=22mm] (circ) at (-3.7, 0) {%
    {\bfseries 0D circulation}\\[4pt]
    \small advance circuit\\
    \small request target volumes%
  };
  \node[card-pink, minimum width=54mm, minimum height=22mm] (mech) at ( 3.7, 0) {%
    {\bfseries 3D mechanics}\\[4pt]
    \small solve equilibrium\\
    \small return cavity pressures%
  };

  % Bidirectional exchange — labelled with the actual symbols.
  \draw[flow-blue, line width=1.0pt]
    ([yshift=4mm]circ.east) --
    node[edge-label, text=uioblue1, above=2pt, pos=0.5]
        {$\mathcal{V}^{*}_\text{LV},\,\mathcal{V}^{*}_\text{RV}$}
    ([yshift=4mm]mech.west);
  \draw[flow-pink, line width=1.0pt]
    ([yshift=-4mm]mech.west) --
    node[edge-label, text=uiopink1, below=2pt, pos=0.5]
        {$p_\text{LV},\,p_\text{RV}$}
    ([yshift=-4mm]circ.east);

  % Active tension — a time-series input, not a peer module. Demoted.
  \draw[flow-orange, line width=0.9pt] (3.7, -1.95) -- (mech.south);
  \node[font=\normalsize, text=uioorange1!85!black, align=center] at (3.7, -2.35)
    {$T_a(t)$\ \ Blanco activation};

  % Recorded outputs — post-processing readouts. Demoted.
  \draw[flow-gray, line width=0.9pt] (mech.north) -- (3.7, 1.95);
  \node[font=\normalsize, text=black!60, align=center] at (3.7, 2.35)
    {stress, strain, work density};

\end{tikzpicture}
\end{document}
